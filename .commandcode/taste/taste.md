- Expects that `gen_html.py` catalog reconciliation adds endpoint models missing from the leaderboard but prunes rows whose model left the endpoint catalog (by-design, commit 362e5ef). `provider/data/tps.csv` is never pruned by reconciliation — historical measurements persist there; only the rendered `index.html` filters rows — so no benchmarked data is ever lost, only hidden from render when the endpoint model disappears. Regeneration must be verified to confirm `provider/data/tps.csv` keeps all entries. Confidence: 0.9- Expects that regenerating output files (e.g., HTML from CSV) must preserve all existing entries in the source CSVs — any regeneration must be verified against git history to confirm no benchmark data rows are silently dropped from the provider CSVs. Confidence: 0.8
- Expects duplicate provider CSV rows (from repeated full re-benchmarks) to collapse to a single row per model, preferring the latest measured row, and a fresh benchmark result to replace the previous row for that model instead of appending. Confidence: 0.9
