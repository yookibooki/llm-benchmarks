- Expects that no existing models or data rows are ever silently dropped — catalog reconciliation (`gen_html.py`) must be add-only: it adds missing endpoint models to `tps.csv` but never removes existing benchmarked rows, even if a model is no longer in the endpoint catalog. Regeneration must be verified to confirm all prior entries remain. Confidence: 0.9- Expects that regenerating output files (e.g., HTML from CSV) must preserve all existing entries — any regeneration must be verified against git history to confirm no models/data rows are silently dropped. Confidence: 0.8
