# TODO

Context: CI benchmarks run daily, but the published leaderboard is stale — `data/tps.csv` and `index.html` were last generated by a manual run on Aug 28 (commit 849ab14). All provider data collected since (Sep 2–3, incl. OpenCode's first measurements) never reached the leaderboard.

## 1. Sync endpoint model catalogs (all providers)

Catalogs rewrite only when the live endpoint hash differs from the snapshot (`shared/snapshot.py`); last CI refresh was Sep 3.

- [x] Load keys and run every filter (safe locally, no benchmark quota):
  ```
  set -a; . ./.env; set +a
  for p in nvidia openrouter google mistral nous opencode; do uv run $p/filter_models.py; done
  ```
- [x] Confirm each `*/data/models.txt` + `*/data/endpoint_snapshot.json` updated where the live catalog changed (hash gate makes unchanged endpoints a no-op). — Mistral regenerated 14 → 8 (live endpoint shrink; 5 stale models + aliases dropped). All other catalogs were already current.
- [x] Commit the regenerated files so the next daily CI run benchmarks newly listed models.

## 2. Leaderboard missing TPS; OpenCode shows no records

### 2.1 `gen_html.py` never runs — why OpenCode is absent

- [x] Wire rendering in: `render()` → `gen_html.main()` called at the end of `run_pipeline()` with failure collection like the other steps.
- [x] Verify locally: `uv run gen_html.py` → `data/tps.csv` + `index.html` regenerated with opencode/nous/mistral measurements.

### 2.2 Duplicate rows accumulate in provider tps.csv

- [x] In `run_provider_benchmark`, seeding collapses duplicate model rows to the latest measured row; a fresh measured result replaces (not appends); a fresh failure keeps the prior measured row (`_keep_prior`).
- [x] `shared/matcher.py::read_rows` dedupes on every match step, regenerating polluted CSVs without benchmark quota.
- [x] Regenerated all six provider CSVs through the fixed code paths (collapsed 2/4/15/6/0/8 duplicate rows; intelligence preserved).

### 2.3 Unmeasured models never get (re-)benchmarked

- [x] Incremental selection is now `_pending_models`: models whose latest row lacks Latency/TPS (no row, error row, placeholder row all qualify).
- [x] Expected refills at live state (post-dedupe): nvidia 7, openrouter 8, nous 3, opencode 5 rows — verified 7/8/3/5 plus google 0, mistral 0.

### 2.4 Post-fix CI run and verification

- [ ] Trigger `workflow_dispatch`; confirm the full pipeline now commits `data/tps.csv` + `index.html` (the `git add` in `daily.yml` already lists them).
- [x] Verify remaining `-` rows are genuine endpoint slowness/failures, not stuck placeholder/error rows — tomorrow's incremental run re-queues exactly the 23 pending models above; remaining `-` rows after that are genuine failures.
- [x] Align docs: AGENTS.md now says 15s first token (matches `HARD_TIMEOUT = 15.0`, deliberately raised from 8 in 1646718).
- [x] Resolve the design conflict: pruning kept (introduced deliberately in 362e5ef, matches current AGENTS.md, test, and only filters the rendered HTML — provider CSVs keep all history); `.commandcode/taste/taste.md` updated to describe pruning + data preservation.

## 3. Doc drift (minor, after 2.x lands)

- [x] AGENTS.md Pipeline section rewritten: 6 providers sequential with per-step exception collection in `shared/pipeline.py`, including the render step.
